# 本轮产品缺陷修复完成报告

> 依据《产品缺陷深度审阅.md》的优先级，逐条落地修复并重建上线。
> 部署地址：**http://localhost:4000**（容器 `kaoshi-app`，镜像 `kaoshi:latest` sha256:8d18658…）

## 一、已修复清单（按严重度）

### 🔴 P0 阻断级（架构/数据错误）
| 项 | 问题 | 修复 |
|---|---|---|
| **P0-1 发布入口统一** | `TeacherDashboard` 走 `POST /api/exams` 只写 exams，缺 timePolicy/allowRedo/theme；`PaperLibrary` 走 assignments。学生只读 exams → Dashboard 发的作业永不限时、不可重做。 | `createExam` 现也写 linked assignment，接收 timePolicy/allowRedo/theme 并落 exam；两条发布路径产出一致体验。 |
| **P0-2 提交按页 merge** | 跨设备交替答题后 `submission.answers` 被整体覆盖，互相丢页。 | `submitExam` 改为按页 `mergeAnswers`，后提交不再清掉前者已写页。 |

### 🟠 P1 体验断裂 / 高频痛点
| 项 | 问题 | 修复 |
|---|---|---|
| **P1-3/4 退出兜底 + 自动同步** | 仅 4s 防抖+翻页触发，退出丢末笔；手动同步反习惯。 | `StudentWorkspace` 加 `beforeunload` / `visibilitychange` 即时落盘（不丢末笔）；点「回大厅」先落盘本地、再静默同步云端（`syncToCloudSilent`，失败不打扰）。 |
| **P1-5 替换原生弹窗** | 大量 `window.confirm/alert` 违反 PRD 第7节，Pad 上割裂。 | 新建 `src/utils/toast.tsx`（全局 Toast + `confirmAsync` 自定义确认弹层）；`App` 挂载 `ToastContainer`/`ConfirmContainer`；`StudentWorkspace` 6 处、`CanvasDraw` 清空确认、App 报告加载失败全部改用 toast —— **学生 Pad 端全链路无原生弹窗**。 |
| **P1-13 家长多孩** | `User.childId` 单值，二孩家庭只能绑一个。 | `childIds: string[]`（兼容旧 `childId`）；login/getMe 返回、parentBindChild 追加、家长查看校验多孩；前端 `ParentDashboard` 多孩切换 tab + 「继续绑定另一个宝贝」入口。 |
| **P1-9/10 批量加人 + 退班/转班** | 逐个加人、无退班/转班入口。 | 后端新增 `removeStudentFromClass` + `DELETE /api/classes/:classId/students/:studentId`；前端学生行加「移出/转班」按钮、转班内联选班面板（退原班+并入目标）、「批量粘贴学号」模式（逐行并入本班）。 |

### 🟡 P2（本轮未做，列出待办）
- 空卷可提交、迟交扣分规则、提交/迟交通知、多设备并发锁、首登改密找回、本地/云端「两种草稿」心智统一
- 教师桌面端的原生 `confirm/alert` 尚未替换（本轮聚焦学生 Pad 高频路径）
- `Submission` 记 `classId` 快照（转班后旧班名依赖班级记录存在）

## 二、验证结果
- 本地 `npm run build`（tsc + vite）通过，0 错误。
- `docker compose build` + `up -d --force-recreate` 成功，`/api/health` 返回 200。
- **端到端冒烟（真实服务）+ 即时清理**：注册临时 teacher → 建两班 → 加学生 → 多班并入(200) → 退班(200「已移出」) → 批量并入(200/200) → 已有账号去重正确(400) → 测试数据用 `better-sqlite3` 全清（删 2 班 + 3 用户，残余 0），生产库无污染。

## 三、改动文件
- 后端：`server/controllers/exams.ts`（createExam 写 assignment、submit 按页 merge、家长多孩校验）、`server/controllers/classes.ts`（removeStudentFromClass）、`server/controllers/auth.ts`（childIds 返回/绑定）、`server/db.ts`（childIds 类型）、`server/index.ts`（DELETE 学生路由）
- 前端：`src/utils/toast.tsx`（新增）、`src/App.tsx`、`src/components/exam/StudentWorkspace.tsx`、`src/components/shared/CanvasDraw.tsx`、`src/components/dashboard/ParentDashboard.tsx`、`src/components/dashboard/TeacherDashboard.tsx`、`src/api.ts`、`src/types.ts`
